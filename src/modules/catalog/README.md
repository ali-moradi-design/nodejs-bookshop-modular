# Catalog module

Owns books, featured listings, cover uploads, and stock rules.

Public API (`index.ts`): `bookRepo`, `bookService`, `IBookRepository`, `Book` types, stock assertions, `registerCatalogRoutes`.
